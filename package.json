{
  "name": "all.this",
  "version": "0.1.0",
  "private": true,
  "description": "The complete neurons.me ecosystem — all.this",
  "author": "suiGn",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "ci": "turbo run ci",
    "validate": "turbo run test && turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@10.33.2"
}